* avoid multiple inclusion of .mk files (i.e. ifndef __prog_mk__ ... endif)
* check preconditions for all the user supplied variables needed by .mk files 
  (i.e. mk_precond function needed)
* fix versioning of mach-o libs
