* make sure you're on a FreeBSD host
* in Makefile, set PORTVERSION as needed
* become root and type "make package"
* if everything was ok there will be a makl-X.Y.Z.tbz file in this directory
* to upload makl package to KL site type "./upload.sh"
